# Required for the newsletter/community forms.
# Local: copy to .dev.vars (Wrangler reads that file).
# Production: wrangler secret put RESEND_API_KEY
RESEND_API_KEY=

# Optional overrides — both default if unset
# NOTIFY_TO_EMAIL=hello@primitiveai.com.au
# NOTIFY_FROM_EMAIL=onboarding@resend.dev
