# Optional — subscribe/thread still persist to D1 without this.
# Local: copy this file to .dev.vars
# Production: wrangler secret put RESEND_API_KEY
RESEND_API_KEY=

# Optional notify overrides
# NOTIFY_TO_EMAIL=hello@primitiveai.com.au
# NOTIFY_FROM_EMAIL=onboarding@resend.dev

# Optional — GitHub OAuth on /login
# Callback URL: https://<host>/api/auth/github/callback
# GITHUB_CLIENT_ID=
# GITHUB_CLIENT_SECRET=
